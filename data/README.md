Place SSH key material here for local/self-hosted deployments.

- `authorized_keys`: allowed public keys for inbound SSH auth.
- `ssh_host_ed25519`: server host key (auto-generated on first start if missing).
